#!/usr/bin/env zsh

# Mise
{{- if lookPath "mise" }}
_init_mise_zsh() {
  eval "$(mise activate zsh)"
}

if (( $+functions[zsh-defer] )); then
  zsh-defer _init_mise_zsh
else
  _init_mise_zsh
fi
{{- end }}
