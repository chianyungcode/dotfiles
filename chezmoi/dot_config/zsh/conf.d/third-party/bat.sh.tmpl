# Theming for bat and delta
{{- $unified_theme := env "UNIFIED_THEME_CLI" -}}
{{- if eq $unified_theme "" }}{{ $unified_theme = .shell_env.common.UNIFIED_THEME_CLI }}{{ end -}}
export BAT_THEME="{{ if eq $unified_theme "kanagawa-dragon" }}kanagawa-dragon{{ else }}gruvbox-dark{{ end }}"

# Pass all help (-h, --help) output through bat
# if [[ -n ${ZSH_NAME} ]]; then
#     alias -g -- -h='-h 2>&1 | bat --language=help --style=plain'
#     alias -g -- --help='--help 2>&1 | bat --language=help --style=plain'
# fi
