# # Skip `eval (tmuxifier init -)` to avoid the extra ~20–30 ms startup cost; copy tmuxifier's completion scripts into Zsh's `completions/` directory so they load lazily instead.
# eval "$(tmuxifier init -)" # tmuxifier completions
