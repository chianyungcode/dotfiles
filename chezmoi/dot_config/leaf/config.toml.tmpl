{{- $unified_theme := env "UNIFIED_THEME_CLI" -}}
{{- if eq $unified_theme "" }}{{ $unified_theme = .shell_env.common.UNIFIED_THEME_CLI }}{{ end -}}

# Managed by chezmoi. Custom themes are resolved relative to this file.
theme = "{{ if eq $unified_theme "kanagawa-dragon" }}kanagawa-dragon.toml{{ else }}ocean{{ end }}"
editor = "nano"
watch = false
