{{- $unified_theme := env "UNIFIED_THEME_CLI" -}}
{{- if eq $unified_theme "" }}{{ $unified_theme = .shell_env.common.UNIFIED_THEME_CLI }}{{ end -}}
set -gx BAT_THEME "{{ if eq $unified_theme "kanagawa-dragon" }}kanagawa-dragon{{ else }}gruvbox-dark{{ end }}"
