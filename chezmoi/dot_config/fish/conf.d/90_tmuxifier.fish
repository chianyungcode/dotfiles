# Skip `eval (tmuxifier init - fish)` to avoid the extra ~20–30 ms startup cost; copy tmuxifier's completion scripts into Fish's `completions/` directory so they load lazily instead.
# eval (tmuxifier init - fish)  # original tmuxifier completions
