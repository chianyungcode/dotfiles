{{ if .use_secrets }}

# If file <key>.pub exist in ~/.ssh_keys directory
{{- if stat (joinPath .chezmoi.homeDir ".ssh_keys" "ghcny.pub") }}
["Code/Personal/Frontend"]
type = "git-repo"
url = "ghcny:allweezy/allweezy.git"
{{ end }}

{{- end }}
